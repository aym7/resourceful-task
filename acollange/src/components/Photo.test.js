import { render, screen } from '@testing-library/react';
import Photo from './Photo';

test('photo main test', () => {
    // Test if valid output while loading
    // Test if photo displayed for the correct id.
    // Test if id does not exist
    // Test successful upload on s3
    // Test failed upload on s3
});

