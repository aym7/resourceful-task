import { render, screen } from '@testing-library/react';
import Uploader from './Uploader';

test('uploader main test', () => {
    // Test if valid if uploads a mp4 < 50MB
    // Test error if type != mp4
    // Test error if size > 50MB
    // Test successful upload on s3
    // Test failed upload on s3
});

